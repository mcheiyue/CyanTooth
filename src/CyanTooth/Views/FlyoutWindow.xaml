<ui:FluentWindow x:Class="CyanTooth.Views.FlyoutWindow"
        x:Name="FlyoutWindowRoot"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:vm="clr-namespace:CyanTooth.ViewModels"
        xmlns:controls="clr-namespace:CyanTooth.Controls"
        mc:Ignorable="d"
        Title="CyanTooth"
        Width="380" Height="520"
        MinWidth="340" MinHeight="400"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        Deactivated="Window_Deactivated"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        
        <Style x:Key="DeviceCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="0,4"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ControlElevationBorderBrush}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ControlFillColorSecondaryBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="16,12,16,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <ui:SymbolIcon Symbol="Bluetooth24" FontSize="24" Margin="0,0,12,0"/>
                <StackPanel>
                    <TextBlock Text="蓝牙" FontSize="18" FontWeight="SemiBold"/>
                    <TextBlock FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}">
                        <Run Text="{Binding ConnectedDeviceCount, Mode=OneWay}"/>
                        <Run Text="个设备已连接"/>
                    </TextBlock>
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <ui:Button Icon="{ui:SymbolIcon ArrowSync24}" 
                          ToolTip="刷新"
                          Command="{Binding RefreshCommand}"
                          Appearance="Transparent"
                          Padding="8"/>
                <ui:Button Icon="{ui:SymbolIcon Settings24}" 
                          ToolTip="设置"
                          Click="Settings_Click"
                          Appearance="Transparent"
                          Padding="8"/>
            </StackPanel>
        </Grid>

        <!-- Search Box -->
        <ui:TextBox Grid.Row="1" 
                   Margin="16,0,16,8"
                   PlaceholderText="搜索设备..."
                   Icon="{ui:SymbolIcon Search24}"
                   Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Device List -->
        <ScrollViewer Grid.Row="2" 
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Disabled"
                     Padding="16,0">

            <ItemsControl ItemsSource="{Binding DevicesView}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <controls:DeviceCard Device="{Binding}"
                                            ConnectCommand="{Binding DataContext.ToggleConnectionCommand, ElementName=FlyoutWindowRoot}"
                                            FavoriteCommand="{Binding DataContext.ToggleFavoriteCommand, ElementName=FlyoutWindowRoot}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Empty State -->
        <StackPanel Grid.Row="2" 
                   VerticalAlignment="Center" 
                   HorizontalAlignment="Center"
                   Visibility="{Binding IsRefreshing, Converter={StaticResource BoolToVis}}">
            <ui:ProgressRing IsIndeterminate="True" Width="40" Height="40"/>
            <TextBlock Text="正在搜索设备..." 
                      Margin="0,12,0,0"
                      Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
        </StackPanel>

        <!-- Footer -->
        <Border Grid.Row="3" 
               BorderThickness="0,1,0,0" 
               BorderBrush="{DynamicResource ControlElevationBorderBrush}"
               Padding="16,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <ui:ToggleSwitch Content="仅显示已连接"
                                    IsChecked="{Binding ShowOnlyConnected}"
                                    FontSize="12"/>
                </StackPanel>

                <ui:Button Grid.Column="1"
                          Content="系统设置"
                          Icon="{ui:SymbolIcon Open24}"
                          Appearance="Transparent"
                          Click="OpenWindowsSettings_Click"
                          FontSize="12"/>
            </Grid>
        </Border>
    </Grid>
</ui:FluentWindow>
